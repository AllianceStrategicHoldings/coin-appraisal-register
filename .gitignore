# macOS
.DS_Store

# Node / Next.js
node_modules/
.next/
out/
dist/
build/

# Env files (never commit API keys)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp

# Local scratch
/scratch/
*.tmp

# Private notes / thinking — never pushed to any shared remote.
# Backed up separately via its own nested git repo (personal only).
/_private/

# PWA build (web/) — explicit, even though patterns above cover them
web/node_modules
web/dist
web/dev-dist
web/.env.local
web/.env*.local
